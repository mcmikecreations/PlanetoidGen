# masters_work

## Notes

The output of the building to 3d model agents needs to be scaled down in blender roughly by 0.656 to get the same outputs as `blender-osm`.
Also, `blender-osm` uses Transverse Mercator projection based on the lat/lon of the entity center, while `PlanetoidGen` uses Web Mercator.