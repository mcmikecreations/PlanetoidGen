namespace PlanetoidGen.Contracts.Constants.StringMessages
{
    public static class IOStringMessages
    {
        private const string Prefix = "IO";

        public static readonly string FormatIncorrect = $"{Prefix}_{nameof(FormatIncorrect)}";
        public static readonly string RequestFailed = $"{Prefix}_{nameof(RequestFailed)}";
    }
}
